import type { Config } from '../types';

export const DEFAULT_CONFIG: Config = {
    testEmail: "angela.temp+demo13@example.com",
    chain: "base-sepolia",
    serverUrl: "https://ping-crossmint-server.vercel.app",
    signerType: "api-key",
};